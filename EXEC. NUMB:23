início
    // Declaração das variáveis
    escrever "Digite o nome:"
    ler nome
    escrever "Digite o sexo (M para masculino ou F para feminino):"
    ler sexo
    escrever "Digite a altura em metros:"
    ler altura
    
    // Verifica o sexo para calcular o peso ideal
    se sexo = 'M' então
        peso_ideal = (72.7 * altura) - 58
    senão
        peso_ideal = (62.1 * altura) - 44.7
    fimse
    
    // Mostra o peso ideal calculado
    escrever "O peso ideal é: ", peso_ideal
fim
