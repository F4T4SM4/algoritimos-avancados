# Inicio
numero_macas = int(input("Digite o número de maçãs compradas: "))

# E SE
if numero_macas < 12:
    preco_unitario = 1.30
else:
    preco_unitario = 1.00

# Calculo
custo_total = numero_macas * preco_unitario

# Resultado
print("O custo total da compra é: R$ {:.2f}".format(custo_total))
